name: example
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:

dev_dependencies:
  test:
  lints:
  dart_test_utils:
    path: ../
